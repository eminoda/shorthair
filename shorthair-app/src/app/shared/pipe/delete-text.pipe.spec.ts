import { DeleteTextPipe } from './delete-text.pipe';

describe('DeleteTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteTextPipe();
    expect(pipe).toBeTruthy();
  });
});
